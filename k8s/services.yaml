apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: med-erp
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: med-erp
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: med-erp
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
